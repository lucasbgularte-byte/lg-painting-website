const menu=document.querySelector('.menu');const nav=document.querySelector('.nav nav');if(menu&&nav){menu.addEventListener('click',()=>nav.classList.toggle('open'));nav.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')))}const form=document.querySelector('#quoteForm');if(form){form.addEventListener('submit',async e=>{e.preventDefault();const data=new FormData(form);const text=[...data.entries()].map(([k,v])=>k+': '+v).join('\n');const notice=document.querySelector('#notice');try{if(navigator.clipboard)await navigator.clipboard.writeText(text);notice.textContent='Enquiry summary copied. Next we can connect this form to the LG Painting business email or WhatsApp.'}catch{notice.textContent='Form ready. Next we can connect it to the LG Painting business email or WhatsApp.'}notice.style.display='block'})}